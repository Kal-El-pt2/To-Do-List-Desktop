/*import { Button, VerticalBox,CheckBox } from "std-widgets.slint";

export component ListItem inherits Window {
    width: 200px;
    height: 25px;
    CheckBox {
        width: parent.width;
        height: parent.height;
        text: "Enter Task Name";
    }
}
export component AppWindow inherits Window {
    ListItem {}
}
*/